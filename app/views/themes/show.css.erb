<% cache [@theme, "css"] do %>
<%= Liquid::Template.parse(@theme.documents.css.map { |d| d.body }.join).render({
		"primary" => @website.primary_colour,
		"secondary" => @website.secondary_colour
		}, :filters => [LiquidFilters]).html_safe %>
<% end %>