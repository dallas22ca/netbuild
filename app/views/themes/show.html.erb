<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @theme.name %>
</p>

<%= link_to 'Back', themes_path %> |

<% if @website.editable_themes.include? @theme %>
	<%= link_to 'Edit', edit_theme_path(@theme) %> |
	<%= link_to "Docs", theme_documents_path(@theme) %>
<% end %>