<%= render "manage_nav" %>

<h1>Member</h1>

<%= render 'form' %>

<% if @membership.user != current_user %>
	<%= link_to "Remove", @membership, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>